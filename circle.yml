machine:
  environment:
    NODE_ENV: development
  node:
    version: v7.7.3
  npm:
    version: 4.1.2
test:
  pre:
    - npm rebuild bcrypt --build-from-source
general:
  artifacts:
    - ./coverage
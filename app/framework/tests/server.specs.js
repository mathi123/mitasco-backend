const expect = require('chai').expect;
const sinon = require('sinon');

describe.skip('Server', () => {
    let var1;

    beforeEach('setup', () => {
        // Arrange
    });

    describe('starting the server', () => {
        it('builds the server instance');
        it('runs the server instance');
        it('initializes the \'application\' property');
        it('enables CORS middleware');
        it('enables JSON parsing middleware');
    });

    describe('building the server', () => {
       it('builds routes of all controllers');
    });

    afterEach('clean up', () => {

    });
});
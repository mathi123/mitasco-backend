const expect = require('chai').expect;
const sinon = require('sinon');

describe('OrmInitializer', () => {
    let var1;

    beforeEach('setup', () => {
        // Arrange
    });

    it('initializes sequelize library with configuration credentials');

    describe('fileFilter', () => {
        it('accepts js files');
        it('rejects index.js files');
    });

    describe('loadModule', () => {
        it('initializes the models on sequelize instance');
        it('initializes the models index.js');
    });

    afterEach(() => {

    });
});